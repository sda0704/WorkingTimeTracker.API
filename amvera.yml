meta:
  environment: csharp
  toolchain:
    name: dotnet
    version: "10.0"
build:
  image: mcr.microsoft.com/dotnet/sdk:10.0
run:
  image: mcr.microsoft.com/dotnet/aspnet:10.0
  buildFileName: WorkingTimeTracker.API
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
